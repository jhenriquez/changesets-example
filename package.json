{
  "name": "changesets-example",
  "version": "1.0.3-rc.0",
  "description": "A basic repository to show case the changeset tool",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"No tests specified\"",
    "version:rc": "changeset pre enter rc && changeset version",
    "version:release": "changeset pre exit && changeset version"
  },
  "keywords": [
    "changesets",
    "example",
    "versioning"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.1",
    "@changesets/cli": "^2.29.7"
  }
}
